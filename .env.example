#
# SGS — exemplo seguro de variáveis (RAIZ)
#
# ESTE ARQUIVO NÃO DEVE CONTER SEGREDOS.
# Use como ponte para os exemplos reais por camada:
# - Backend local/dev: `backend/.env.example`
# - Backend produção: configure no provedor e use `backend/.env.production.example` como referência
# - Frontend local/dev: `frontend/.env.local.example`
#
# Não copie este arquivo para produção. Ele existe apenas para onboarding e descoberta de variáveis.
#

# Flags locais (opcional)
LOCAL_FEATURE_AI_ENABLED=false
LOCAL_APR_ANALYTICS_ENABLED=false
LOCAL_MFA_ENABLED=true
