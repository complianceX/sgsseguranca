# Copiar para ops/docker/redis/.env na VPS (nunca commitar - já coberto por .gitignore).
# Gerar cada senha separadamente: openssl rand -base64 32
SGS_REDIS_AUTH_PASSWORD=
SGS_REDIS_RATELIMIT_PASSWORD=
SGS_REDIS_CACHE_PASSWORD=
SGS_REDIS_QUEUE_PASSWORD=
